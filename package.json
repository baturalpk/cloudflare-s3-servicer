{
  "private": true,
  "name": "cloudflare-s3-servicer",
  "version": "0.1.0",
  "main": "index.js",
  "scripts": {
    "fmt": "prettier --ignore-path .gitignore --write \"**/*.+(js|ts|json|md)\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "prettier": "2.7.1"
  },
  "dependencies": {
    "aws4fetch": "^1.0.13"
  }
}
